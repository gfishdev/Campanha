spring:
  application:
    name: nsGuilherme
  datasource:
    url: jdbc:mysql://localhost:3306/NS_GUILHERME
    username: root
    password: lira10
  jpa:
    hibernate.ddl-auto: validate
    show-sql: true
    properties.hibernate.format_sql: true